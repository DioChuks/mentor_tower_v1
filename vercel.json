{
  "version": 2,
  "builds": [
    { "src": "api/index.ts", "use": "@now/node", "config": { "maxLambdaSize": "15mb" } }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/public/$1" },
    { "src": "/(.*)", "dest": "/api/index.ts" }
  ]
}